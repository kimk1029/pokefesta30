node_modules
.next
out
.env
.env.local
.env.*.local
.DS_Store
*.tsbuildinfo

# 모바일 빌드 산출물 — 소스만 트랙, 빌드 결과물은 무시.
# (mobile/.gitignore 와 중복이지만 root git 에서도 확실히 가드)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/dist-android/
mobile/web-build/
mobile/android/
mobile/ios/
mobile/.env
mobile/.env.local
mobile/.env.*.local

